spring:
    application:
        name: muscle-map
    
    datasource:
        url: jdbc:postgresql://localhost:5432/muscle_map_db
        username: postgres
        password: Ronaldo7
        driver-class-name: org.postgresql.Driver

    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect

    #Used for database migration in offline apps, not needed in this case.
    # flyway:
    #     enabled: true
    #     baseline-on-migrate: true


server:
    port: 8080  